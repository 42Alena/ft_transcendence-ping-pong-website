export const GAME_TICK_RATE = 60;       // Game loop rate in Hz
export const PADDLE_SPEED = 5;          // Paddle speed (pixels per tick)
export const BALL_SPEED = 6;            // Initial ball speed
export const GAME_WIDTH = 800;          // Canvas width
export const GAME_HEIGHT = 600;         // Canvas height
