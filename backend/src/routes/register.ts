/* 

here will be all checks and send to chlss if all correct. Class work swith already checked parameters

async registerUserWithPassword(input: Types.UserRegister): Promise<User> {

			const username = input.username.trim();
			const displayName  = input.displayName.trim();
			const avatarUrl  = input.avatarUrl.trim();
			const passwordPlain  = input.username.trim();

TODO:
check pass
*/